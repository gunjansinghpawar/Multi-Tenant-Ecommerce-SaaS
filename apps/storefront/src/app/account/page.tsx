import { OverviewTab } from '../../components/account/tabs/overview-tab';

export default function AccountOverviewPage() {
  // The root /account path always renders the Overview Tab
  return <OverviewTab />;
}
